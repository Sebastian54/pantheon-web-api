# Used by docker-compose.yml at deploy time (Proxmox host .env)
POSTGRES_USER=pantheon
POSTGRES_PASSWORD=
POSTGRES_DB=pantheon

# Cloudflare Tunnel token (from `cloudflared tunnel token <tunnel-name>`)
CLOUDFLARE_TUNNEL_TOKEN=
